name: E2E Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "latest"
      - name: Install dependencies
        run: |
          uv sync --all-extras
          uv venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH
          # Install test dependencies manually since uv sync may not install them
          pip install testcontainers ruamel.yaml
      - name: Run tests
        run: pytest tests/ -v --tb=short